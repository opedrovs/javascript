// Aqui configuramos nossas permissões usuários

const { resolve } = require('path');

module.exports = {
  config: resolve(__dirname, 'src', 'config', 'database.js'), // arquivo de configuração
  'models-path': resolve(__dirname, 'src', 'models'), // caminho dos meus models
  'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'), // Caminho das migrations, que seria basicamente alterações da nossa base de dados, por exemplo, criar uma tabela, porque o sequelize vai ter um histórico de alterações na nossa base de dados
  'seeders-path': resolve(__dirname, 'src', 'database', 'seeds'),
};
